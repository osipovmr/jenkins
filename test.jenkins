#!groovy

properties([disableConcurrentBuilds()])
pipeline {
    agent any
    tools {
        maven 'Maven 3.8.4'
    }
    options {
        timestamps()
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build JAR') {
            steps {
                withMaven {
                    sh "mvn clean package"
                }
            }
        }
    }
}